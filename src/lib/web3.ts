// Minimal web3/wagmi config stub for local development.
// If you don't configure wallets or wagmi, export `null` so components
// can detect the absence of a config and avoid rendering Wagmi providers.

export const config: any = null;

export default config;
